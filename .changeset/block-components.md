---
"@reluna-ui/ui": minor
---

Add reusable layout block components (AppHeader, PageHeader, AppFooter) and implement atomic design pattern for templates. Blocks can be imported and customized via props for consistent page layouts across the application.
